# Dependências de execução — Tech Challenge Fase 2
# Instalação: pip install -r requirements.txt
# Requer Java 17 ou 21 no PATH (exigência do Apache Spark 4).

pyspark==4.0.1
delta-spark==4.0.0        # Delta Lake; o jar é resolvido via Maven na 1ª execução
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
PyYAML==6.0.2
kafka-python==2.0.2       # produtor de eventos (perna streaming)
matplotlib==3.9.2         # gráficos dos notebooks
scikit-learn==1.5.2       # modelo de referência do notebook de IA
